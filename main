-- Tworzenie GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "SilentPackGui"
screenGui.Parent = game:GetService("CoreGui") -- Używamy CoreGui, żeby nie zniknęło po śmierci postaci

-- Czarny Frame (Tło)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(1, 0, 1, 0)
mainFrame.BackgroundColor3 = Color3.new(0, 0, 0)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- Napis "NEW SILENT PACK VERSION"
local title = Instance.new("TextLabel")
title.Text = "NEW SILENT PACK VERSION"
title.Size = UDim2.new(0, 400, 0, 50)
title.Position = UDim2.new(0.5, -200, 0.5, -25)
title.BackgroundTransparency = 1
title.TextColor3 = Color3.new(1, 1, 1)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.Parent = mainFrame

-- Przycisk COPY DISCORD
local copyButton = Instance.new("TextButton")
copyButton.Text = "COPY DISCORD"
copyButton.Size = UDim2.new(0, 200, 0, 50)
copyButton.Position = UDim2.new(0.5, -100, 0.9, -25)
copyButton.BackgroundColor3 = Color3.fromRGB(88, 101, 242) -- Kolor Discorda
copyButton.TextColor3 = Color3.new(1, 1, 1)
copyButton.Font = Enum.Font.Gotham
copyButton.TextSize = 20
copyButton.Parent = mainFrame

-- Zaokrąglenie przycisku
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = copyButton

--- LOGIKA TWEENU (Latający napis) ---
local TweenService = game:GetService("TweenService")
local info = TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true)

-- Cel animacji (lekkie przesuwanie się w górę i dół)
local goals = {
    Position = UDim2.new(0.5, -200, 0.45, -25)
}

local tween = TweenService:Create(title, info, goals)
tween:Play()

--- LOGIKA KOPIOWANIA ---
copyButton.MouseButton1Click:Connect(function()
    local discordLink = "https://discord.gg/vtzxB3cMPf" -- TUTAJ WPISZ SWÓJ LINK
    
    if setclipboard then
        setclipboard(discordLink)
        copyButton.Text = "COPIED!"
        task.wait(2)
        copyButton.Text = "COPY DISCORD"
    else
        copyButton.Text = "ERR: NO EXECUTOR"
    end
end)
